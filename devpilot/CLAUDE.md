## Agent skills

### Issue tracker

Issues 作为本地 Markdown 文件存放在 `.scratch/<feature>/` 目录下。参见 `docs/agents/issue-tracker.md`。

### Triage labels

五个标准标签映射到 issue 文件中的 `Status:` 行。参见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文：仓库根目录下一个 `CONTEXT.md` + `docs/adr/`。参见 `docs/agents/domain.md`。

### ignore
任何操作都忽略first-ts，这只是一个我自用的测试，tmp文件夹